doctype html
html(lang="ko")
    head
        title #{pageTitle} | Wetube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header
            h1=pageTitle
            nav 
                ul 
                    li
                        a(href="/") 🏠 Home
                    if loggedIn
                        li
                            a(href="/users/logout") 🚪 Log out
                        li
                            a(href="/") #{loggedInUser.name}의 프로파일
                    else
                        li
                            a(href="/join") 🤝 Join
                        li
                            a(href="/login") 🚪 Login
                    li
                        a(href="/search") 🔍 Search
                    li
                        a(href="/videos/upload") 📽️ Upload Video
        main
            block content
    include partials/footer